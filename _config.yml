markdown: CommonMarkGhPages
commonmark:
  options: ["UNSAFE"]
  extensions: ["table"]
kramdown:
   syntax_highlighter_opts:
      disable : true
sass:
  style: :compressed
theme: jekyll-theme-slate
defaults:
  -
    scope:
      path: "stages"
    values:
      layout: "stage"
